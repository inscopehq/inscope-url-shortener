{
  "name": "inscope-url-shortener",
  "version": "1.0.0",
  "description": "A coding exercise for building a URL shortener service with React frontend, Express API, and SQLite database",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd frontend && npm start",
    "server": "cd backend && npm run dev",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "test-setup": "node test-setup.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["url-shortener", "react", "express", "sqlite", "coding-exercise"],
  "author": "InScope",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
